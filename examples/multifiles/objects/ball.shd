import objects.paddles;

buf updated = false;
pub buf pub_buffer = 10;

pub buf fn update_ball() {
    var paddle_pos = paddle_position(0u);
    updated = true;
    update_child_pub(); // ok
    update_child_private(); // ERROR: private item
}
