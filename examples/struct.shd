struct Object {
    position: Vec,
    value: i32,
    size: Vec,
}

struct Vec {
    x: i32,
    y: i32,
    z: i32,
}

buf obj = Object(Vec(0, 0, 0), 10, Vec(1, 1, 1));

run {
    obj = Object(Vec(5, 2, 7), 11, Vec(3, 4, 8));
}
