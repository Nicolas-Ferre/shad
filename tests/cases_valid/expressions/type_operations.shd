buf alignof_custom_struct = alignof(Custom);
buf alignof_native_struct = alignof(u32x3);
buf sizeof_custom_struct = sizeof(Custom);
buf sizeof_native_struct = sizeof(u32x3);

struct Custom {
    field1: u32x2,
    field2: u32,
}
