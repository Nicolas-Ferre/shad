buf data = Data(vec4i(3, 4, 5, 6), i32x2(1, 2));

run {
    data.v4.y = 7;
}

gpu struct vec4i {
    x: i32,
    y: i32,
    z: i32,
    w: i32,
}

gpu(vec2i) struct i32x2 {
    x: i32,
    y: i32,
}

struct Data {
    v4: vec4i,
    v2: i32x2,
}

gpu fn vec4i(x: i32, y: i32, z: i32, w: i32) -> vec4i;
gpu(vec2i) fn i32x2(x: i32, y: i32) -> i32x2;
