buf f32_val = 0.;
buf i32_val = 0;

run {
    f32_val = i32_val;
    var local_f32_val = 0.;
    local_f32_val = i32_val;
}

fn invalid_param_assignment(param: f32) {
    param = 0;
}

fn invalid_returned_type() -> undef {
    return 0;
}

run {
    var value = 0;
    value = invalid_returned_type();
}
