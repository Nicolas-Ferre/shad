fn func(a: i32, b: u32) {
}

fn func2(v: i32) -> i32 {
    return v;
}

run {
    func(0, 0u);
    func(0, 0);
    func(0);
    other_func();
    0.func(0u);
    0.func();
    func2(func2(func2()));
    // 0.func2().func2().func2().func2(1); // TODO: enable
}
